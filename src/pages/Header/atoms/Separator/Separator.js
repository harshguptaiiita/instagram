import React from 'react';
import Icon from '../../../../atoms/Icons';
const LeftHeader = (props) => {

  return (
    <div>
      <Icon url = {props.url} />
    </div>
  );
}

export default LeftHeader;
